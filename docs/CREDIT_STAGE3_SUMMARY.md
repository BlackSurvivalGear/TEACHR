# Stage 3 summary

The migration model now converts the six legacy per-tool remaining allowances into one universal Credit balance, preserves admin-added allowance, marks migration version 1, creates a migration ledger entry, and skips already-migrated accounts. Live generation enforcement remains unchanged pending cutover.
